{
  "name": "fastapi-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "while true; do .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000; sleep 2; done",
    "seed": ".venv/bin/python scripts/seed.py"
  }
}
