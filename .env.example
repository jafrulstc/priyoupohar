# Next.js frontend env (all data flows through the FastAPI backend)
# FastAPI base URL used by /api/media/* proxy (default: http://localhost:8000)
FASTAPI_URL="http://localhost:8000"

# NOTE: backend config (database, JWT, S3/R2 storage) lives in
# mini-services/fastapi-backend/.env — see .env.example there.
